import 'package:flutter/material.dart';

class TermodinamicaScreen extends StatelessWidget {
  const TermodinamicaScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(
        child: Text("Bajo desarrollo"),
      ),
    );
  }
}
